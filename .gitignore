# Personal resume data backup
resume_bak.yaml

# Generated PDFs
*.pdf
!demo/*.pdf

# LaTeX auxiliary files (kept in build/)
*.aux
*.log
*.out

# Build directory (intermediate files like .tex, .log)
build/

# Resume output directory (final PDFs and backups)
resume/

# Python virtual environments
venv/
env/
.env

# System files
.DS_Store

# YAML files (except resume_data.yaml in src/)
*.yaml
!src/resume_data.yaml